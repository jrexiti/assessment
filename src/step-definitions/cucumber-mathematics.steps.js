const { Given, When, Then } = require("@cucumber/cucumber");

Given("I have {int} cucumbers", function (amount) {
  return "pending";
});

When("I eat {int} cucumbers", function (amount) {
  return "pending";
});

Then("I have {int} cucumbers", function (amount) {
  return "pending";
});

Given("I have {int} carrots", function (amount) {
  return "pending";
});

When("I eat {int} carrots", function (amount) {
  return "pending";
});

Then("I have {int} carrots", function (amount) {
  return "pending";
});

Then("I have {int} vegetables", function (amount) {
  return "pending";
});
